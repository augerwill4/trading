# gex-lab runtime dependencies.
#
# Floors rather than exact pins: this is a personal research tool, and nothing
# here uses an API that moves between these majors. The version actually
# verified in this environment is in the trailing comment, so if something
# breaks later you know what it was built against.
pandas>=2.2        # verified 3.0.6
numpy>=1.26        # verified 2.4.6
scipy>=1.11        # verified 1.17.1
requests>=2.31     # verified 2.34.2
matplotlib>=3.8    # verified 3.11.2
# Fallback data source only. Approved explicitly on 2026-09-18 as an exception
# to the minimal-dependency rule; CLAUDE.md's dependency list was updated to match.
yfinance>=0.2.40   # verified 1.7.0
